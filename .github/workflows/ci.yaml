name: ci
on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches:
      - main
jobs:
  go-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: Run linters
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.4
          args: --timeout=3m
  go-test:
    strategy:
      matrix:
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: go tests
        run: go test -v -covermode=count -json ./... > test.json
      - name: annotate go tests
        if: always()
        uses: guyarb/golang-test-annotations@v0.8.0
        with:
          test-results: test.json
  sync-test:
    runs-on: ubuntu-latest
    env:
      BATON_LOG_LEVEL: debug
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: Build baton-demo
        run: go build ./cmd/baton-demo
      - name: Initialize database
        run: ./baton-demo --init-db
      - name: Grant/revoke scoped role
        uses: ConductorOne/github-workflows/actions/sync-test@v4
        with:
          connector: ./baton-demo
          baton-entitlement: 'scoped_role:role-0000000:project-0000000:assignment'
          baton-principal: 'user-0000001'
          baton-principal-type: 'user'
      - name: Grant/revoke
        uses: ConductorOne/github-workflows/actions/sync-test@v4
        with:
          connector: ./baton-demo
          baton-entitlement: 'group:group-0000001:member'
          baton-principal: 'user-0000000'
          baton-principal-type: 'user'
      - name: Test account creation
        uses: ConductorOne/github-workflows/actions/account-provisioning@v4
        with:
          connector: ./baton-demo
          account-email: testuser1@example.com
          account-login: testuser1
          account-profile: '{"username": "testuser1", "email": "testuser1@example.com", "firstName": "test", "lastName": "user1"}'
